mode: single # single or dev-and-prod

webhook:
  name: ns-preview

tls:
  secretName: preview-ns-tls

host:
  base: ns-preview.trapti.tech

preview:
  imagePattern: "ghcr.io/traptitech/ns-dashboard:preview-{{number}}-{{head_sha}}"

backend:
  matcher: PathPrefix(`/neoshowcase.protobuf.APIService`)
  service:
    kind: Service
    # namespace: ns-system
    # name: ns-gateway
    # port: http
    # scheme: h2c

staging:
  enabled: true
  image: ghcr.io/traptitech/ns-dashboard:main@sha256:bca39f07db33677db072e6bd9dfa548ecc1fb1bc3a6e4cc35e373ca27fd653f3

otherParameters:
  port: 80
