{{- with $.Values.frontend }}
apiVersion: v1
kind: Service
metadata:
  name: {{ $.Release.Name }}-frontend
  namespace: {{ $.Release.Namespace }}

spec:
  type: ClusterIP
  selector:
    app: {{ $.Release.Name }}-frontend
  ports:
    - name: http
      port: 80
      targetPort: http

{{- end }}