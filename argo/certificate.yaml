apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: argo-workflow

spec:
  issuerRef:
    kind: ClusterIssuer
    name: cluster-issuer
  secretName: argo-workflow-tls
  duration: 2160h0m0s # 90d
  renewBefore: 720h0m0s # 30d
  dnsNames:
    - workflow.toki317.dev
