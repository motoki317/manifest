apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: rancher

spec:
  issuerRef:
    kind: ClusterIssuer
    name: cluster-issuer
  secretName: rancher-tls
  duration: 2160h0m0s # 90d
  renewBefore: 360h0m0s # 15d
  dnsNames:
    - "rancher.toki317.dev"
