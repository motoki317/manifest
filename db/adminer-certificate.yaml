apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: adminer

spec:
  issuerRef:
    kind: ClusterIssuer
    name: cluster-issuer
  secretName: adminer-tls
  duration: 2160h0m0s # 90d
  renewBefore: 360h0m0s # 15d
  dnsNames:
    - adminer.toki317.dev
