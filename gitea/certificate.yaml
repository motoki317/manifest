apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: gitea

spec:
  issuerRef:
    kind: ClusterIssuer
    name: cluster-issuer
  secretName: gitea-tls
  duration: 2160h0m0s # 90d
  renewBefore: 360h0m0s # 15d
  dnsNames:
    - git.toki317.dev
