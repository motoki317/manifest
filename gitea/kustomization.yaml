resources:
- certificate.yaml
- ingress-route.yaml
- service.yaml
- ssh-ingress-route.yaml
- stateful-set.yaml

configMapGenerator:
- files:
  - config/app.ini
  name: config

images:
- name: gitea
  newName: gitea/gitea
  newTag: 1.19.3
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
