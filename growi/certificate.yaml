apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: growi

spec:
  issuerRef:
    kind: ClusterIssuer
    name: cluster-issuer
  secretName: growi-tls
  duration: 2160h0m0s # 90d
  renewBefore: 360h0m0s # 15d
  dnsNames:
    - wiki.toki317.dev
