apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: dashboard

spec:
  issuerRef:
    kind: ClusterIssuer
    name: cluster-issuer
  secretName: dashboard-tls
  duration: 2160h0m0s # 90d
  renewBefore: 360h0m0s # 15d
  dnsNames:
    - k8s-dashboard.toki317.dev
