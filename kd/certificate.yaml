apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: kd

spec:
  issuerRef:
    kind: ClusterIssuer
    name: cluster-issuer
  secretName: kd-tls
  duration: 2160h0m0s # 90d
  renewBefore: 720h0m0s # 30d
  dnsNames:
    - kd.toki317.dev
