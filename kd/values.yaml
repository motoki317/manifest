# The auth-admin edge already restricts access to admins, so the chart's default
# policy (every authenticated user is a viewer) needs no further narrowing.
ingressRoute:
  enabled: true
  host: kd.toki317.dev
  middlewares:
    - name: auth-admin
      namespace: auth
  tls:
    secretName: kd-tls
