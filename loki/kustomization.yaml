resources:
  - service.yaml
  - service-memberlist.yaml
  - stateful-set.yaml

configMapGenerator:
  - name: config
    files:
      - config/config.yaml
  - name: runtime-config
    files:
      - config/runtime-config.yaml
