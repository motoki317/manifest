resources:
  - service.yaml
  - statefulset.yaml
  - sshd-role.yaml
  - sshd-role-binding.yaml
  - sshd-service.yaml
  - sshd-service-account.yaml
  - sshd-statefulset.yaml

configMapGenerator:
  - name: authorized-keys
    files:
      - config/authorized_keys
