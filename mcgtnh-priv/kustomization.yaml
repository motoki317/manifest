resources:
  - service.yaml
  - statefulset.yaml
  - sshd-service.yaml
  - sshd-service-account.yaml
  - sshd-statefulset.yaml

configMapGenerator:
  - name: authorized-keys
    files:
      - config/authorized_keys
