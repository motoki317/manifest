resources:
  - cluster-role.yaml
  - cluster-role-binding.yaml
  - daemon-set-entrypoint.yaml
  - daemon-set-services.yaml
  - service-account.yaml

configMapGenerator:
  - name: beyla-entrypoint
    files:
      - ./config/beyla-entrypoint.yaml
  - name: beyla-services
    files:
      - ./config/beyla-services.yaml
