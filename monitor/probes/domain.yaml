apiVersion: operator.victoriametrics.com/v1beta1
kind: VMProbe
metadata:
  name: domain

spec:
  jobName: domain
  vmProberSpec:
    url: domain-exporter.monitor.svc.cluster.local
  interval: 5m
  scrapeTimeout: 10s
  targets:
    staticConfig:
      targets:
        - toki317.dev
