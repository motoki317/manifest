resources:
  - cluster-role.yaml
  - cluster-role-binding.yaml
  - deployment.yaml
  - service.yaml
  - service-account.yaml

configMapGenerator:
  - name: prometheus
    files:
      - config/prometheus.yml
