apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: pushgateway

spec:
  issuerRef:
    kind: ClusterIssuer
    name: cluster-issuer
  secretName: pushgateway-tls
  duration: 2160h0m0s # 90d
  renewBefore: 360h0m0s # 15d
  dnsNames:
    - pushgateway.toki317.dev
