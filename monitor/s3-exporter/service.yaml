apiVersion: v1
kind: Service
metadata:
  name: s3-exporter
  annotations:
    prometheus.io/probe: "true"

spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      name: http
  selector:
    app: s3-exporter
