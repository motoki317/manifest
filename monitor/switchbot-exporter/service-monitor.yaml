apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: switchbot-exporter

spec:
  selector:
    matchLabels:
      app: switchbot-exporter
  endpoints:
    - port: http
      interval: 1m
