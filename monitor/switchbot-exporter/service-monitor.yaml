apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: switchbot-exporter
  labels:
    vm-archive: "true"

spec:
  selector:
    matchLabels:
      app: switchbot-exporter
  endpoints:
    - port: http
      interval: 1m
