apiVersion: v1
kind: Service
metadata:
  name: switchbot-exporter
  labels:
    app: switchbot-exporter

spec:
  type: ClusterIP
  selector:
    app: switchbot-exporter
  ports:
    - name: http
      port: 80
      targetPort: http
