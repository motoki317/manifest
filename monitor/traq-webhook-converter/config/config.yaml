template: |
  ### [{{ .commonLabels.severity | upper }}] {{ .commonAnnotations.summary }}{{ if eq .status "firing" }} (x{{ .alerts | len }}){{ end }}
  *Runbook:* {{ .commonAnnotations.runbook_url }}
  {{- range $idx, $alert := .alerts }}
  - {{ .annotations.description }}
  {{- end }}
