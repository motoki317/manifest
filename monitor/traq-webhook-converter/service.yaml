apiVersion: v1
kind: Service
metadata:
  name: traq-webhook-converter

spec:
  type: ClusterIP
  selector:
    app: traq-webhook-converter
  ports:
    - name: http
      port: 80
      targetPort: http
