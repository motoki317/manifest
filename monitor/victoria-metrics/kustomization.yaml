resources:
  - cluster-role.yaml
  - cluster-role-binding.yaml
  - service.yaml
  - service-account.yaml
  - stateful-set.yaml

configMapGenerator:
  - name: victoria-metrics
    files:
      - config/victoria-metrics.yml
