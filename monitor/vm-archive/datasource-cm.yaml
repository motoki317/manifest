apiVersion: v1
kind: ConfigMap
metadata:
  name: vm-archive-datasource
  labels:
    grafana_datasource: "1"

data:
  vm-archive.yaml: |
    apiVersion: 1
    datasources:
      - name: vm-archive
        type: prometheus
        access: proxy
        url: http://vmsingle-vm-archive.monitor.svc.cluster.local.:8428
