helmCharts:
  - repo: https://netdata.github.io/helmchart
    name: netdata
    version: "3.7.110"
    includeCRDs: true
    namespace: netdata
    releaseName: netdata
    valuesFile: values.yaml

generators:
  - ksops.yaml
