apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: ns-preview

spec:
  issuerRef:
    kind: ClusterIssuer
    name: cluster-issuer
  secretName: ns-preview-tls
  duration: 2160h0m0s # 90d
  renewBefore: 360h0m0s # 15d
  dnsNames:
    - "*.ns-preview.toki317.dev"
