helmCharts:
  - repo: oci://ghcr.io/traptitech/charts
    name: neoshowcase
    version: "1.9.4"
    includeCRDs: true
    namespace: ns-system
    releaseName: ns
    valuesFile: values.yaml

resources:
  - app-namespace.yaml
  - certificate.yaml

generators:
  - ksops.yaml
