apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: portainer

spec:
  issuerRef:
    kind: ClusterIssuer
    name: cluster-issuer
  secretName: portainer-tls
  duration: 2160h0m0s # 90d
  renewBefore: 360h0m0s # 15d
  dnsNames:
    - "portainer.toki317.dev"
