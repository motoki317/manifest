apiVersion: v1
kind: Service
metadata:
  name: ns-dashboard

spec:
  type: ClusterIP
  selector:
    # referenced in kustomize replacements
    app: ns-dashboard
  ports:
    - port: 80
      targetPort: http
      name: http
