apiVersion: v1
kind: Service
metadata:
  name: pushgateway
  labels:
    app: pushgateway

spec:
  type: ClusterIP
  selector:
    app: pushgateway
  ports:
    - name: http
      port: 80
      targetPort: http
