apiVersion: v1
kind: Service
metadata:
  name: rollout-bluegreen-active

spec:
  selector:
    app: rollout-bluegreen
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
