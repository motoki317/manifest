resources:
  - cluster-role.yaml
  - cluster-role-binding.yaml
  - deployment.yaml
  - middleware-dynamic.yaml
  - service.yaml
  - service-account.yaml

configMapGenerator:
  - name: sablier
    files:
      - config/sablier.yaml
