resources:
  - cluster-role.yaml
  - cluster-role-binding.yaml
  - deployment.yaml
  - service.yaml
  - service-account.yaml

configMapGenerator:
  - name: sablier
    files:
      - config/sablier.yaml
