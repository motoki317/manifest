apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: auth-admin

spec:
  issuerRef:
    kind: ClusterIssuer
    name: cluster-issuer
  secretName: auth-admin-tls
  duration: 2160h0m0s # 90d
  renewBefore: 360h0m0s # 15d
  dnsNames:
    - auth-admin.toki317.dev
