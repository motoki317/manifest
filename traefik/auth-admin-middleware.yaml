apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: auth-admin

spec:
  forwardAuth:
    address: http://auth-admin.traefik.svc.cluster.local/
    authResponseHeaders:
      - X-Forwarded-User
