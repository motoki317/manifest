apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-all

spec:
  replicas: 1
  selector:
    matchLabels:
      app: auth-all
  template:
    metadata:
      labels:
        app: auth-all
    spec:
      containers:
        - name: auth-admin
          image: ghcr.io/jordemort/traefik-forward-auth:latest
          args:
            - --config=/config.ini
          ports:
            - containerPort: 4181
              name: http
          volumeMounts:
            - name: ns-config
              mountPath: /config.ini
              subPath: ini-all
      volumes:
        - name: ns-config
          secret:
            secretName: forward-auth
