apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: dashboard

spec:
  issuerRef:
    kind: ClusterIssuer
    name: cluster-issuer
  secretName: traefik-dashboard-tls
  duration: 2160h # 90d
  renewBefore: 360h # 15d
  dnsNames:
    - traefik-dashboard.toki317.dev
